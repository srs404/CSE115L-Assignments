#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char a[50], b[50];
    printf("Enter String: ");
    gets(a);

    revstr(a);
}

char revstr(char a[50])
{
    if(a[i] != '\0')
    {

    }
}
